# 22 Nisan 2026

## Gunluk Amac

Bu dosya 22 Nisan 2026 tarihinde `HubPreview`, `MainMenu Rotator`, editor-side tuning ve ray editor uzerinde yapilan teknik degisikliklerin dogrulanmis kaydidir. Amac, ayni gun icinde cozulmus veya tekrar acilmamis konulari ertesi gun yeniden kesfetmemek, mimari karar zincirini korumak ve acik isleri net ayirmaktir.

Bu gunluk iki ana oturum bolumune ayrilir:

- `Birinci 5 Saatlik Oturumda Yapilanlar`
- `Ikinci 5 Saatlik Oturumda Yapilanlar`

Gun sonunda bu dosya yeniden taranip guncellenecektir.

## Taranan Kaynaklar

Bu gunluk hazirlanirken dogrudan taranan ana dosyalar:

- `Assets/WordSpinAlpha/Scripts/Editor/WordSpinAlphaSceneBuilder.cs`
- `Assets/WordSpinAlpha/Scripts/Presentation/LevelHubPreviewController.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/LevelHubPreviewControllerEditor.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfile.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfileEditor.cs`
- `Assets/WordSpinAlpha/Generated/EditorData/HubPreviewLayoutTuningProfile.asset`
- `Assets/WordSpinAlpha/Docs/CODEX_HIGH_MINI_WORKFLOW.md`

Not:

- Worktree kirli oldugu icin `git diff --name-only` ciktisinda bugunun konusu olmayan baska dosyalar da vardir.
- Bu gunluk yalnizca dosya ustunden dogrulanan 22 Nisan editor/tuning/ray duzeltmelerini kapsar.

## Birinci 5 Saatlik Oturumda Yapilanlar

### 1. HubPreview node mimarisi temizlendi

Dogru mimari `NodeVisual child` olarak sabitlendi.

Dogru sonuc:

- `LevelHubPreviewController` hierarchy mutasyonu yapmiyor.
- `WordSpinAlphaSceneBuilder` tek gorsel mimariyi uretiyor.
- `OnValidate` / `ExecuteAlways` uzerinden hierarchy uretme modeli kaldirildi.

Ilgili dosyalar:

- `Assets/WordSpinAlpha/Scripts/Presentation/LevelHubPreviewController.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/WordSpinAlphaSceneBuilder.cs`

### 2. HubPreview scene/prefab override kirliligi temizlendi

Ana bozulma kaynagi sahne/prefab override kirliligi idi.

Dogru sonuc:

- `HubPreview` sahnesi ve `LevelHubPreview.prefab` temiz kaynaga geri cekildi.
- orphan `NodeVisual` ve prefab drift sinifi temizlendi.

Ilgili dosyalar:

- `Assets/WordSpinAlpha/Scenes/HubPreview.unity`
- `Assets/WordSpinAlpha/Generated/Prefabs/LevelHubPreview.prefab`
- `Assets/WordSpinAlpha/Scripts/Editor/WordSpinAlphaSceneBuilder.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewSceneNormalizer.cs`

### 3. Beyaz zemin sorunu kapatildi

Kesin sebep:

- `LevelNode_*` root `Image` bileşeni host olarak kalmasi gerekirken alpha zincirine girip gorunur hale geliyordu.

Dogru sonuc:

- host `Image` gorunmez host olarak tutuldu
- alpha yalnizca gercek gorsellere gitti
- `NodeVisual` child render katmani olarak kaldi

Ilgili dosyalar:

- `Assets/WordSpinAlpha/Scripts/Presentation/LevelHubPreviewController.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/WordSpinAlphaSceneBuilder.cs`
- `Assets/WordSpinAlpha/Generated/Prefabs/LevelHubPreview.prefab`

### 4. HubPreview ray state rebuild sirasinda korunur hale getirildi

Kesin sebep:

- plain rebuild yolu scene controller state'ini capture etmeden prefab default `railPoints` ile sahneyi yeniden kuruyordu.

Dogru sonuc:

- `RebuildLevelHubPreviewScene()` rebuild oncesi state capture ediyor
- yeni scene kurulduktan sonra state geri apply ediliyor
- plain rebuild artik ray'i sessizce sifirlamiyor

Ilgili dosya:

- `Assets/WordSpinAlpha/Scripts/Editor/WordSpinAlphaSceneBuilder.cs`

### 5. Play mode scene restore hatasi kapatildi

Kesin sebep:

- `EditorSceneManager.NewScene(...)` play mode sirasinda tetiklenebiliyordu.

Dogru sonuc:

- restore/rebuild guard'lari play mode ve compile/update anlarinda calismayacak sekilde sertlestirildi

Ilgili dosya:

- `Assets/WordSpinAlpha/Scripts/Editor/WordSpinAlphaSceneBuilder.cs`

### 6. Editor-owned layout tuning altyapisi kuruldu

Amaç:

- `Alttas` ve sonra `MainMenu Rotator` gibi generated preview katmanlarini scene override biriktirmeden ayarlayabilmek

Dogru sonuc:

- layout verisi editor-owned asset'e tasindi
- builder bu veriyi okumaya basladi
- fallback mantigi korundu

Ilgili dosyalar:

- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfile.cs`
- `Assets/WordSpinAlpha/Generated/EditorData/HubPreviewLayoutTuningProfile.asset`
- `Assets/WordSpinAlpha/Scripts/Editor/WordSpinAlphaSceneBuilder.cs`

### 7. MainMenu Rotator aynı tuning modulune baglandi

Dogru sonuc:

- profil icine `MainMenuRotator` eklendi
- builder `ResolveMainMenuRotator()` okumaya basladi
- `Rotator` tuning, runtime rotator sistemine dokunmadan editor-owned kaldı

Ilgili dosyalar:

- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfile.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfileEditor.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/WordSpinAlphaSceneBuilder.cs`

### 8. Tuning workflow'u yeniden kuruldu

Kesin sorun:

- generic raw capture + hemen full rebuild modeli hem `Alttas` hem `Rotator` icin kirilgandi

Dogru sonuc:

- `Capture` ile `Rebuild` ayrildi
- `Capture Selected + Apply` full rebuild tetiklemiyor
- profile + prefab source apply modeli korundu
- `MainMenu Rotator` aspect-safe normalize edildi

Ilgili dosyalar:

- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfile.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfileEditor.cs`

### 9. Ray editor dogrulugu kapatildi

Kesin sorunlar:

- editor ray cizimi spline degil polyline idi
- kullanici `LevelNode_*` kutularini yanlislikla secip surukleyebiliyordu

Dogru sonuc:

- editor ray cizimi spline sampling ile node yoluyla cakisacak sekilde duzeltildi
- node secimi, ray edit aktifken editor kontrolune alindi
- bu is `Alttas`/`Rotator` tuning dosyalarina dokunulmadan yapildi

Ilgili dosyalar:

- `Assets/WordSpinAlpha/Scripts/Presentation/LevelHubPreviewController.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/LevelHubPreviewControllerEditor.cs`

### 10. Alttas Scene View UX iki adimda tamamlandi

#### 10.1 Perspektif / boyut ilüzyonu

Kesin tespit:

- sorun veri modeli degil, Scene View edit dogrulama yuzeyi idi
- `Alttas` ScreenSpaceOverlay canvas uzerinde editlenirken perspektif bakis ozellikle width/height degisiminde yaniltici goruntu uretiyordu

Dogru sonuc:

- `Select Alttas + Align View` akisina editor-side Scene View hizalama eklendi
- hizalama yalnizca `Alttas` section'ina baglandi

#### 10.2 Scene View resize affordance

Kesin tespit:

- Inspector'da width/height vardi
- fakat sahnede boyutlandirma kullaniciya acik ve net gosteren ozel handle yoktu

Dogru sonuc:

- `Alttas` seciliyken Scene View'da width ve height icin ayri handle cizildi
- resize dogrudan `RectTransform.sizeDelta` ustunden calisiyor
- Undo, prefab instance modification ve scene dirty isaretleme var

Ilgili dosya:

- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfileEditor.cs`

### 11. Workflow dokumani guncellendi

22 Nisan verisine gore workflow'a iki kalici kural eklendi:

1. `Editor-side gorev paketleme kurali`
2. `Model secim ve maliyet kurali`

Dogru sonuc:

- coupled editor task'ler tek mini promptta birlestirilmeyecek
- model secimi artik `beklenen prompt sayisi x regresyon riski` ile yapilacak

Ilgili dosya:

- `Assets/WordSpinAlpha/Docs/CODEX_HIGH_MINI_WORKFLOW.md`

## 22 Nisan Sonunda Netlesen Teknik Dersler

### A. Ayni anda bagli editor davranislarini degistirmek regresyon uretir

Ozellikle mini model icin su siniflar tek promptta birlestirilmemeli:

- editor workflow semantics
- prefab source apply
- scene rebuild
- multi-element tuning
- context finder

### B. `Capture` ile `Rebuild` birbirine baglanirsa kirilgan editor araci olusur

Bu gun bunu dogrulayan iki alan:

- `HubPreview` layout tuning
- ray edit / scene rebuild etkileşimi

### C. `HubPreview` ve `MainMenu` farkli preview context'leridir

Tek tuning modulu icinde yonetilebilseler de:

- finder
- scene targeting
- rebuild hedefi
ayri ayri dogrulanmalidir.

### D. Scene View UX sorunu veri sorunu degildir

`Alttas` boyut ilüzyonu bunu kanitladi.

Dogru tepki:

- builder veya runtime zincirine dokunmamak
- editor UX katmaninda cozum aramak

## Ikinci 5 Saatlik Oturumda Yapilanlar

Bu bolum ikinci 5 saatlik oturumda dosya ustunden tekrar taranip kapatilan veya yeniden tanimlanan isleri tasir.

### 1. `Alttas` reset/default semantigi yeniden kuruldu

Kesin sorun:

- `Reset Alttas To Defaults + Apply` hard-coded sabite donuyor ve apply'i ayni anda yapiyordu
- bu, tasarim referansini takip etmeyi zorlastiriyor ve yanlislikla anlik sahne degisikligi riski uretiyordu

Dogru sonuc:

- `current/live` ile `baseline/reference` ayrimi profile'a eklendi
- reset semantigi baseline-first calisacak sekilde baglandi
- `Reset` ile `Apply` ayrildi
- `Set Current As Baseline` editor aksiyonu eklendi
- kullanici `Alttas` tuning'ini tek panelden yonetir hale geldi

Ilgili dosyalar:

- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfile.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfileEditor.cs`
- `Assets/WordSpinAlpha/Generated/EditorData/HubPreviewLayoutTuningProfile.asset`

### 2. `MainMenu Rotator` phase 4 yeniden tanimlandi

Kesin karar:

- `Rotator` icin inspector merkezli tuning parity degil
- scene-side live edit ana akis olacak

Neden:

- kullanici `MainMenu` sahnesinde `Rotator`u dogrudan secip tasimak ve boyutlandirmak istiyor
- ekstra `Apply` adimi ana akis olmamali
- builder/runtime zinciri zaten mevcut current tuning verisini okuyor

### 3. `MainMenu Rotator` scene-side live move tamamlandi

Kesin sorun:

- `Rotator` icin mevcut tuning altyapisi vardi
- fakat scene icinde dogrudan tasima yapilip sonucun aninda kalici olmasi saglanmiyordu

Dogru sonuc:

- `Rotator` seciliyken Scene View icinde live move handle eklendi
- drag boyunca scene instance ve current tuning guncelleniyor
- mouse-up aninda profile current degeri assete kaydoluyor
- mevcut prefab-source apply helper ile prefab kaynagi da guncelleniyor
- ekstra `Apply` bu akis icin zorunlu olmadi

Ilgili dosya:

- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfileEditor.cs`

### 4. `MainMenu Rotator` scene-side live resize tamamlandi

Kesin sorun:

- `Rotator` icin move eklense bile boyutlandirma ayni canli akis icinde yoktu
- kullanici sahnede dogrudan boyut degisikligi yapip play modda hemen gormek istiyordu

Dogru sonuc:

- `Rotator` seciliyken Scene View icinde width-only live resize handle eklendi
- resize current tuning'e aninda yaziliyor
- scene objesi canli olarak yeniden apply ediliyor
- mouse-up aninda asset ve prefab source commit ediliyor
- `NormalizeInPlace(image)` uzerinden aspect-safe davranis korundugu icin squash olusmuyor

Ilgili dosya:

- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfileEditor.cs`

### 5. Script Updater kaynakli iki compile kirigi temizlendi

Kesin sorun:

- Unity Script Updater, `HubPreviewLayoutTuningProfileEditor.cs` icinde iki handle satirini hatali donusturdu
- biri `Handles.FreeMoveHandle(...)` cevresinde gecersiz ara degisken uretimi
- digeri `Vector2.Lerp(..., target.pivot)` benzeri yanlis API kullanimi

Dogru sonuc:

- script updater artiklari temizlendi
- anchored position hesabi component bazli `Mathf.Lerp` ile duzeltildi
- console tekrar temiz hale getirildi

Ilgili dosya:

- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfileEditor.cs`

### 6. Phase 4 kapatildi

Kapanis kriterleri:

- `Rotator` scene icinde tasinabiliyor
- `Rotator` scene icinde boyutlandirilabiliyor
- ekstra `Apply` ana akis olmadan sonuc kalici oluyor
- play modda son konum ve son boyut goruluyor
- `Alttas` akisi bozulmuyor
- ray editor etkilenmiyor
- builder/runtime zinciri degismiyor

Durum:

- phase 4 kapatildi

### Ikinci 5 Saatlik Oturum Sonunda Acik Kalan Tek Madde

#### `Reset Alttas To Defaults + Apply` isminin ve son davranis kararinin kapanisi

Durum:

- temel semantik problem cozuldu
- baseline/current modeli kuruldu
- reset/apply ayrildi
- ancak buton ismi ve artik tarihsel hale gelen eski adlandirma tamamen kapatildi mi sorusu ertesi gun netlestirilebilir

Bu madde blocker degil.

## Gun Sonu Guncelleme Kuralı

Bu dosya 22 Nisan gunu sonunda tekrar taranacak.

Gun sonu guncellemede su kontroller yeniden yapilacak:

- birinci 5 saatlik oturumda yazilan maddeler dosya ustunden tekrar dogrulandi mi
- ikinci 5 saatlik oturum maddelerinde degisiklik oldu mu
- acik kalan isler kapandi mi
- ertesi gun tekrar etmemesi gereken yeni ders cikti mi

## Kisa Ozet

22 Nisan sonunda:

- `HubPreview` sahne/prefab kirliligi temizlendi
- node host/image ve beyaz zemin sorunu kapatildi
- ray state rebuild zinciri korundu
- play mode restore hatasi kapatildi
- editor-owned tuning sistemi kuruldu
- `MainMenu Rotator` tuning ayni sisteme tasindi
- tuning workflow ve ray editor dogrulugu duzeltildi
- `Alttas` Scene View alignment ve resize UX tamamlandi
- workflow dokumani editor-side paketleme, model secim ve gunluk tutma kurallariyla guncellendi
- `Alttas` baseline/current reset semantigi kuruldu
- `MainMenu Rotator` phase 4 live move + live resize ile kapatildi

Acik kalan:

- `Reset Alttas To Defaults + Apply` adlandirma ve son karar kapanisi

## Gun Sonu Revizyonu: Phase 5 Bottom Navigation Bar

22 Nisan gun sonu taramasinda `HubPreview` altina eklenecek bottom navigation bar icin ayrica bir Phase 5 akisi baslatildi. Bu bolum, gun sonunda dosya ustunden netlesen son durumu ve 23 Nisan'a devreden fazlari kaydeder.

### 1. Ilk Phase 5 denemesi tek turda yapildi ve reddedildi

Kesin sorun:

- bottom navigation bar tek turda `builder + tuning + scene-side live edit` olarak uygulanmaya calisildi
- bu, 22 Nisan workflow dersine aykiri bir hareketti
- sahnede nav goruntu kalitesi kabul edilemez oldu ve `OynaBg` cevresinde yanlis yerlesim ortaya cikti

Dogru sonuc:

- bu uygulama kabul edilmedi
- Phase 5 yeniden fazlara bolundu

### 2. Phase 5 yeniden fazlandi

Gun sonunda kabul edilen yeni sira:

1. `Phase 1`: yalniz Phase 5 degisikliklerini geri alma
2. `Phase 2`: builder-only static navigation bar
3. `Phase 3`: tuning contract + safe selection
4. `Phase 4`: scene-side live move
5. `Phase 4 devam`: scene-side live resize

Kesin karar:

- bundan sonra bu is tek turda uygulanmayacak
- her faz kendi dogrulamasi ile kapanacak

### 3. Phase 1 rollback tamamlandi

Yalniz Phase 5 kapsamindaki bottom nav kodlari geri alindi.

Geri alinan nav izleri:

- `HubPreviewLayoutTuningProfile.cs` icindeki `HubPreviewBottomPageNav` kontrati
- `HubPreviewLayoutTuningProfileEditor.cs` icindeki bottom nav inspector ve live edit yolları
- `WordSpinAlphaSceneBuilder.cs` icindeki onceki nav yapisi

Dogru sonuc:

- `HubPreview`, nav eklenmeden onceki onayli duzene geri dondu
- `Alttas`, ray editor ve `MainMenu Rotator` akislari korunarak rollback yapildi

Ilgili dosyalar:

- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfile.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/HubPreviewLayoutTuningProfileEditor.cs`
- `Assets/WordSpinAlpha/Scripts/Editor/WordSpinAlphaSceneBuilder.cs`

### 4. Phase 2 builder-only static nav kuruldu

Gun sonunda kod tabaninda su durum dogrulandi:

- `WordSpinAlphaSceneBuilder.cs` icinde `BottomPageNav` yeniden eklendi
- `FrameImage` icin `RawImage` + `Texture2D` + `uvRect` kullaniliyor
- highlight katmani icinde:
  - `ActiveAdventureGlow` acik
  - `ActiveMissionsGlow` kapali
  - `ActiveStoreGlow` kapali
  - `ActiveProfileGlow` kapali
- sibling sirasi:
  - `Background`
  - `Alttas`
  - `PathContainer`
  - `BottomPageNav`
  - `OynaBg`

Ilgili dosya:

- `Assets/WordSpinAlpha/Scripts/Editor/WordSpinAlphaSceneBuilder.cs`

### 5. Phase 2 gorsel sorunu tesbit edildi ve blocker olarak acik kaldi

Unity dogrulamasinda:

- nav bar sahnede gorundu
- `Macera` glow soldaki ilk slotta gorundu
- ancak nav kenarlarinda beyaz/soluk fringe kaldi

Kesin dosya ustu tespit:

- mevcut nav asset import ayarlarinda `alphaIsTransparency: 1` gorunuyor
- buna ragmen final PNG piksel alpha degerleri opak cikti (`A=255`)
- builder nav'i `RawImage + uvRect` ile ciziyor
- bu zincir beyaz/opak kenar pikselleri oldugu gibi ekrana tasiyor

Kesin sonuc:

- problem yalniz builder degil
- problem yalniz Photoshop arka plan kaldirma hissi de degil
- final PNG teknik olarak gercek transparan degil
- builder render yolu da ideal degil

### 6. Gun sonunda kabul edilen teknik karar

Bu konu icin iki net karar alindi:

1. mevcut nav asset gercek transparan PNG olarak yeniden uretilmeden Phase 2 kapanmayacak
2. temiz asset geldikten sonra builder tarafi `RawImage + uvRect` yerine `Image + Sprite` tabanli yola alinacak

Neden:

- yarin tasarim PNG'si degistiginde sistemin asset'e ozel crop/uv hack tasimamasi gerekiyor
- temiz ana asset + ayri glow overlay mimarisi daha degisiklige dayanikli

## 23 Nisan 2026'ya Devreden Fazlar

### Phase 2 duzeltme

Baslamadan once:

- nav asset gercek transparan PNG olarak tekrar alinacak
- Unity icinde yeniden kontrol edilecek

Sonra:

- `WordSpinAlphaSceneBuilder.cs` icindeki nav render yolu `RawImage + uvRect`ten `Image + Sprite` tarafina cekilecek
- hedef, beyaz fringe'i kapatmak ve asset degisimini daha dayanikli hale getirmek

### Phase 3

`HubPreviewBottomPageNav` icin:

- tuning contract
- safe selection
- inspector section
- `Select Scene Bottom Nav`
- `Capture Selected + Apply`

Bu faz 22 Nisan'da baslatilmadi.

### Phase 4

Iki alt faz olarak kaldi:

1. bottom nav scene-side live move
2. bottom nav scene-side live resize

Bu fazlar 22 Nisan'da baslatilmadi.

## 22 Nisan Gun Sonu Kapanis Durumu

22 Nisan sonunda kesin durum:

- `HubPreview`, `Alttas`, ray editor ve `MainMenu Rotator` taraflarinda onceki kapanan fazlar temiz kaldi
- Phase 5 yalnizca `builder-only static nav` noktasina kadar getirildi
- Phase 5, temiz transparan asset ve builder render duzeltmesi olmadan ilerletilmeyecek
- Photoshop / yeni PNG alma asamasindan sonraki tum Phase 5 adimlari 23 Nisan'a ertelendi
